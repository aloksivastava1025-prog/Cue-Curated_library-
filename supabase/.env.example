# CUE — server-side secrets (edge functions + local dev proxy).
# Copy to `supabase/.env` and fill in real values. Never commit.

# Anthropic — used by admin AI autofill (server-side only).
ANTHROPIC_API_KEY=sk-ant-api03-...
